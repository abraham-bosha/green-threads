#pragma once

/**
 * @brief Hard-wired architecture memory constants and memory size primitives.
 */

/**
 * @brief Standard virtual memory page size layout boundary for x86_64 architectures (4KB).
 */
#define GT_PAGE_SIZE 4096UL
